const NotFound = () => {
  document.title = "Not found | Empire Developers";

  return (
    <h1 className="my-2 text-2xl font-raleway font-semibold text-center">
      Sorry, but the page you are looking for does not exist.
    </h1>
  );
};

export default NotFound;
